[pytest]
testpaths = tests
# tests/ is on sys.path so `from conftest import ...` works in every module.
pythonpath = . tests
addopts = -q --strict-markers
markers =
    slow: boots the app in a subprocess (seconds, not milliseconds)
filterwarnings =
    ignore::DeprecationWarning
